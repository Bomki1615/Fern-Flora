const state={
    basketItemsItems: {}
}
const getters={
    basket: (state) => state.basketItems,
}
const actions={

}
const mutations={

}

export default{
    state,
    getters,
    actions,
    mutations,
}